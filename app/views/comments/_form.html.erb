<div class="input-group">
  <%= simple_form_for([@post, @post.comments.build]) do |f| %>
    <%= f.input :content, label: false, input_html: {rows: 5, cols: 75}, class:  "form-control" %><br/>
    <%= f.button :submit, class: 'btn btn-primary margin-tb-15' %>
  <% end %>
</div>
